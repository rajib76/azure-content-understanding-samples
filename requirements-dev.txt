# Only needed to regenerate the files in samples/. They are committed, so skip
# this unless you want to change them.
#   .venv/bin/pip install -r requirements-dev.txt

-r requirements.txt

reportlab==5.0.1            # typeset PDF fixtures
