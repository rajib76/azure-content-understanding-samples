# Only needed to regenerate the files in samples/. The samples are committed,
# so skip this unless you want to change them.
#   .venv/bin/pip install -r requirements-dev.txt

-r requirements.txt

# PDF fixtures
reportlab==5.0.1

# .xlsx fixtures
openpyxl==3.1.5
